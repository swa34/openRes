services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - server/.env
    environment:
      - PORT=3000
    restart: unless-stopped
